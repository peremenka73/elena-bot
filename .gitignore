# Секреты — никогда не публикуются в git
.env

# База данных — своя на каждом сервере, в git не хранится
data.db

# Служебные файлы Python
__pycache__/
*.pyc

# Гайды — не публикуются в git (репозиторий публичный), загружаются на сервер отдельно
guides/*.pdf
